package org.example;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class SnsLambdaHandler {
    private static final Logger LOGGER = LogManager.getLogger(SnsLambdaHandler.class);
}
